import { PageShell } from "../../components/PageShell";
import { Typography } from "@mui/material";

export default function PayrollPage() {
  return <PageShell title="Расчёт заработной платы"><Typography>Модуль расчёта зарплаты — в разработке</Typography></PageShell>;
}
